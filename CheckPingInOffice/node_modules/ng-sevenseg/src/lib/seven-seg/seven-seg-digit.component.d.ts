import { AfterViewInit, QueryList, Renderer2, ElementRef } from '@angular/core';
export declare class SevenSegDigitComponent implements AfterViewInit {
    private renderer;
    segments: QueryList<ElementRef>;
    point: any;
    class: string;
    private _digitValue;
    private _showDecimal;
    allSegments: Array<number>;
    digit: number;
    showDecimal: boolean;
    idx: number;
    constructor(renderer: Renderer2);
    ngAfterViewInit(): void;
    render(): void;
}
